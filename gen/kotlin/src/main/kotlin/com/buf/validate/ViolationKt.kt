// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: buf/validate/validate.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package com.buf.validate;

@kotlin.jvm.JvmName("-initializeviolation")
public inline fun violation(block: com.buf.validate.ViolationKt.Dsl.() -> kotlin.Unit): com.buf.validate.Violation =
  com.buf.validate.ViolationKt.Dsl._create(com.buf.validate.Violation.newBuilder()).apply { block() }._build()
/**
 * ```
 * `Violation` represents a single instance where a validation rule, expressed
 * as a `Rule`, was not met. It provides information about the field that
 * caused the violation, the specific rule that wasn't fulfilled, and a
 * human-readable error message.
 *
 * For example, consider the following message:
 *
 * ```proto
 * message User {
 * int32 age = 1 [(buf.validate.field).cel = {
 * id: "user.age",
 * expression: "this < 18 ? 'User must be at least 18 years old' : ''",
 * }];
 * }
 * ```
 *
 * It could produce the following violation:
 *
 * ```json
 * {
 * "ruleId": "user.age",
 * "message": "User must be at least 18 years old",
 * "field": {
 * "elements": [
 * {
 * "fieldNumber": 1,
 * "fieldName": "age",
 * "fieldType": "TYPE_INT32"
 * }
 * ]
 * },
 * "rule": {
 * "elements": [
 * {
 * "fieldNumber": 23,
 * "fieldName": "cel",
 * "fieldType": "TYPE_MESSAGE",
 * "index": "0"
 * }
 * ]
 * }
 * }
 * ```
 * ```
 *
 * Protobuf type `buf.validate.Violation`
 */
public object ViolationKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: com.buf.validate.Violation.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
      @kotlin.PublishedApi
      internal fun _create(builder: com.buf.validate.Violation.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
    internal fun _build(): com.buf.validate.Violation = _builder.build()

    /**
     * ```
     * `field` is a machine-readable path to the field that failed validation.
     * This could be a nested field, in which case the path will include all the parent fields leading to the actual field that caused the violation.
     *
     * For example, consider the following message:
     *
     * ```proto
     * message Message {
     * bool a = 1 [(buf.validate.field).required = true];
     * }
     * ```
     *
     * It could produce the following violation:
     *
     * ```textproto
     * violation {
     * field { element { field_number: 1, field_name: "a", field_type: 8 } }
     * ...
     * }
     * ```
     * ```
     *
     * `optional .buf.validate.FieldPath field = 5 [json_name = "field"];`
     */
    public var field: com.buf.validate.FieldPath
      @JvmName("getField")
      get() = _builder.field
      @JvmName("setField")
      set(value) {
        _builder.field = value
      }
    /**
     * ```
     * `field` is a machine-readable path to the field that failed validation.
     * This could be a nested field, in which case the path will include all the parent fields leading to the actual field that caused the violation.
     *
     * For example, consider the following message:
     *
     * ```proto
     * message Message {
     * bool a = 1 [(buf.validate.field).required = true];
     * }
     * ```
     *
     * It could produce the following violation:
     *
     * ```textproto
     * violation {
     * field { element { field_number: 1, field_name: "a", field_type: 8 } }
     * ...
     * }
     * ```
     * ```
     *
     * `optional .buf.validate.FieldPath field = 5 [json_name = "field"];`
     */
    public fun clearField() {
      _builder.clearField()
    }
    /**
     * ```
     * `field` is a machine-readable path to the field that failed validation.
     * This could be a nested field, in which case the path will include all the parent fields leading to the actual field that caused the violation.
     *
     * For example, consider the following message:
     *
     * ```proto
     * message Message {
     * bool a = 1 [(buf.validate.field).required = true];
     * }
     * ```
     *
     * It could produce the following violation:
     *
     * ```textproto
     * violation {
     * field { element { field_number: 1, field_name: "a", field_type: 8 } }
     * ...
     * }
     * ```
     * ```
     *
     * `optional .buf.validate.FieldPath field = 5 [json_name = "field"];`
     * @return Whether the field field is set.
     */
    public fun hasField(): kotlin.Boolean {
      return _builder.hasField()
    }
    public val ViolationKt.Dsl.fieldOrNull: com.buf.validate.FieldPath?
      get() = _builder.fieldOrNull

    /**
     * ```
     * `rule` is a machine-readable path that points to the specific rule that failed validation.
     * This will be a nested field starting from the FieldRules of the field that failed validation.
     * For custom rules, this will provide the path of the rule, e.g. `cel[0]`.
     *
     * For example, consider the following message:
     *
     * ```proto
     * message Message {
     * bool a = 1 [(buf.validate.field).required = true];
     * bool b = 2 [(buf.validate.field).cel = {
     * id: "custom_rule",
     * expression: "!this ? 'b must be true': ''"
     * }]
     * }
     * ```
     *
     * It could produce the following violations:
     *
     * ```textproto
     * violation {
     * rule { element { field_number: 25, field_name: "required", field_type: 8 } }
     * ...
     * }
     * violation {
     * rule { element { field_number: 23, field_name: "cel", field_type: 11, index: 0 } }
     * ...
     * }
     * ```
     * ```
     *
     * `optional .buf.validate.FieldPath rule = 6 [json_name = "rule"];`
     */
    public var rule: com.buf.validate.FieldPath
      @JvmName("getRule")
      get() = _builder.rule
      @JvmName("setRule")
      set(value) {
        _builder.rule = value
      }
    /**
     * ```
     * `rule` is a machine-readable path that points to the specific rule that failed validation.
     * This will be a nested field starting from the FieldRules of the field that failed validation.
     * For custom rules, this will provide the path of the rule, e.g. `cel[0]`.
     *
     * For example, consider the following message:
     *
     * ```proto
     * message Message {
     * bool a = 1 [(buf.validate.field).required = true];
     * bool b = 2 [(buf.validate.field).cel = {
     * id: "custom_rule",
     * expression: "!this ? 'b must be true': ''"
     * }]
     * }
     * ```
     *
     * It could produce the following violations:
     *
     * ```textproto
     * violation {
     * rule { element { field_number: 25, field_name: "required", field_type: 8 } }
     * ...
     * }
     * violation {
     * rule { element { field_number: 23, field_name: "cel", field_type: 11, index: 0 } }
     * ...
     * }
     * ```
     * ```
     *
     * `optional .buf.validate.FieldPath rule = 6 [json_name = "rule"];`
     */
    public fun clearRule() {
      _builder.clearRule()
    }
    /**
     * ```
     * `rule` is a machine-readable path that points to the specific rule that failed validation.
     * This will be a nested field starting from the FieldRules of the field that failed validation.
     * For custom rules, this will provide the path of the rule, e.g. `cel[0]`.
     *
     * For example, consider the following message:
     *
     * ```proto
     * message Message {
     * bool a = 1 [(buf.validate.field).required = true];
     * bool b = 2 [(buf.validate.field).cel = {
     * id: "custom_rule",
     * expression: "!this ? 'b must be true': ''"
     * }]
     * }
     * ```
     *
     * It could produce the following violations:
     *
     * ```textproto
     * violation {
     * rule { element { field_number: 25, field_name: "required", field_type: 8 } }
     * ...
     * }
     * violation {
     * rule { element { field_number: 23, field_name: "cel", field_type: 11, index: 0 } }
     * ...
     * }
     * ```
     * ```
     *
     * `optional .buf.validate.FieldPath rule = 6 [json_name = "rule"];`
     * @return Whether the rule field is set.
     */
    public fun hasRule(): kotlin.Boolean {
      return _builder.hasRule()
    }
    public val ViolationKt.Dsl.ruleOrNull: com.buf.validate.FieldPath?
      get() = _builder.ruleOrNull

    /**
     * ```
     * `rule_id` is the unique identifier of the `Rule` that was not fulfilled.
     * This is the same `id` that was specified in the `Rule` message, allowing easy tracing of which rule was violated.
     * ```
     *
     * `optional string rule_id = 2 [json_name = "ruleId"];`
     */
    public var ruleId: kotlin.String
      @JvmName("getRuleId")
      get() = _builder.ruleId
      @JvmName("setRuleId")
      set(value) {
        _builder.ruleId = value
      }
    /**
     * ```
     * `rule_id` is the unique identifier of the `Rule` that was not fulfilled.
     * This is the same `id` that was specified in the `Rule` message, allowing easy tracing of which rule was violated.
     * ```
     *
     * `optional string rule_id = 2 [json_name = "ruleId"];`
     */
    public fun clearRuleId() {
      _builder.clearRuleId()
    }
    /**
     * ```
     * `rule_id` is the unique identifier of the `Rule` that was not fulfilled.
     * This is the same `id` that was specified in the `Rule` message, allowing easy tracing of which rule was violated.
     * ```
     *
     * `optional string rule_id = 2 [json_name = "ruleId"];`
     * @return Whether the ruleId field is set.
     */
    public fun hasRuleId(): kotlin.Boolean {
      return _builder.hasRuleId()
    }

    /**
     * ```
     * `message` is a human-readable error message that describes the nature of the violation.
     * This can be the default error message from the violated `Rule`, or it can be a custom message that gives more context about the violation.
     * ```
     *
     * `optional string message = 3 [json_name = "message"];`
     */
    public var message: kotlin.String
      @JvmName("getMessage")
      get() = _builder.message
      @JvmName("setMessage")
      set(value) {
        _builder.message = value
      }
    /**
     * ```
     * `message` is a human-readable error message that describes the nature of the violation.
     * This can be the default error message from the violated `Rule`, or it can be a custom message that gives more context about the violation.
     * ```
     *
     * `optional string message = 3 [json_name = "message"];`
     */
    public fun clearMessage() {
      _builder.clearMessage()
    }
    /**
     * ```
     * `message` is a human-readable error message that describes the nature of the violation.
     * This can be the default error message from the violated `Rule`, or it can be a custom message that gives more context about the violation.
     * ```
     *
     * `optional string message = 3 [json_name = "message"];`
     * @return Whether the message field is set.
     */
    public fun hasMessage(): kotlin.Boolean {
      return _builder.hasMessage()
    }

    /**
     * ```
     * `for_key` indicates whether the violation was caused by a map key, rather than a value.
     * ```
     *
     * `optional bool for_key = 4 [json_name = "forKey"];`
     */
    public var forKey: kotlin.Boolean
      @JvmName("getForKey")
      get() = _builder.forKey
      @JvmName("setForKey")
      set(value) {
        _builder.forKey = value
      }
    /**
     * ```
     * `for_key` indicates whether the violation was caused by a map key, rather than a value.
     * ```
     *
     * `optional bool for_key = 4 [json_name = "forKey"];`
     */
    public fun clearForKey() {
      _builder.clearForKey()
    }
    /**
     * ```
     * `for_key` indicates whether the violation was caused by a map key, rather than a value.
     * ```
     *
     * `optional bool for_key = 4 [json_name = "forKey"];`
     * @return Whether the forKey field is set.
     */
    public fun hasForKey(): kotlin.Boolean {
      return _builder.hasForKey()
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun com.buf.validate.Violation.copy(block: `com.buf.validate`.ViolationKt.Dsl.() -> kotlin.Unit): com.buf.validate.Violation =
  `com.buf.validate`.ViolationKt.Dsl._create(this.toBuilder()).apply { block() }._build()

public val com.buf.validate.ViolationOrBuilder.fieldOrNull: com.buf.validate.FieldPath?
  get() = if (hasField()) getField() else null

public val com.buf.validate.ViolationOrBuilder.ruleOrNull: com.buf.validate.FieldPath?
  get() = if (hasRule()) getRule() else null

