// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: buf/validate/validate.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package com.buf.validate;

@kotlin.jvm.JvmName("-initializeoneofRules")
public inline fun oneofRules(block: com.buf.validate.OneofRulesKt.Dsl.() -> kotlin.Unit): com.buf.validate.OneofRules =
  com.buf.validate.OneofRulesKt.Dsl._create(com.buf.validate.OneofRules.newBuilder()).apply { block() }._build()
/**
 * ```
 * The `OneofRules` message type enables you to manage rules for
 * oneof fields in your protobuf messages.
 * ```
 *
 * Protobuf type `buf.validate.OneofRules`
 */
public object OneofRulesKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: com.buf.validate.OneofRules.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
      @kotlin.PublishedApi
      internal fun _create(builder: com.buf.validate.OneofRules.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
    internal fun _build(): com.buf.validate.OneofRules = _builder.build()

    /**
     * ```
     * If `required` is true, exactly one field of the oneof must be present. A
     * validation error is returned if no fields in the oneof are present. The
     * field itself may still be a default value; further rules
     * should be placed on the fields themselves to ensure they are valid values,
     * such as `min_len` or `gt`.
     *
     * ```proto
     * message MyMessage {
     * oneof value {
     * // Either `a` or `b` must be set. If `a` is set, it must also be
     * // non-empty; whereas if `b` is set, it can still be an empty string.
     * option (buf.validate.oneof).required = true;
     * string a = 1 [(buf.validate.field).string.min_len = 1];
     * string b = 2;
     * }
     * }
     * ```
     * ```
     *
     * `optional bool required = 1 [json_name = "required"];`
     */
    public var required: kotlin.Boolean
      @JvmName("getRequired")
      get() = _builder.required
      @JvmName("setRequired")
      set(value) {
        _builder.required = value
      }
    /**
     * ```
     * If `required` is true, exactly one field of the oneof must be present. A
     * validation error is returned if no fields in the oneof are present. The
     * field itself may still be a default value; further rules
     * should be placed on the fields themselves to ensure they are valid values,
     * such as `min_len` or `gt`.
     *
     * ```proto
     * message MyMessage {
     * oneof value {
     * // Either `a` or `b` must be set. If `a` is set, it must also be
     * // non-empty; whereas if `b` is set, it can still be an empty string.
     * option (buf.validate.oneof).required = true;
     * string a = 1 [(buf.validate.field).string.min_len = 1];
     * string b = 2;
     * }
     * }
     * ```
     * ```
     *
     * `optional bool required = 1 [json_name = "required"];`
     */
    public fun clearRequired() {
      _builder.clearRequired()
    }
    /**
     * ```
     * If `required` is true, exactly one field of the oneof must be present. A
     * validation error is returned if no fields in the oneof are present. The
     * field itself may still be a default value; further rules
     * should be placed on the fields themselves to ensure they are valid values,
     * such as `min_len` or `gt`.
     *
     * ```proto
     * message MyMessage {
     * oneof value {
     * // Either `a` or `b` must be set. If `a` is set, it must also be
     * // non-empty; whereas if `b` is set, it can still be an empty string.
     * option (buf.validate.oneof).required = true;
     * string a = 1 [(buf.validate.field).string.min_len = 1];
     * string b = 2;
     * }
     * }
     * ```
     * ```
     *
     * `optional bool required = 1 [json_name = "required"];`
     * @return Whether the required field is set.
     */
    public fun hasRequired(): kotlin.Boolean {
      return _builder.hasRequired()
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun com.buf.validate.OneofRules.copy(block: `com.buf.validate`.OneofRulesKt.Dsl.() -> kotlin.Unit): com.buf.validate.OneofRules =
  `com.buf.validate`.OneofRulesKt.Dsl._create(this.toBuilder()).apply { block() }._build()

