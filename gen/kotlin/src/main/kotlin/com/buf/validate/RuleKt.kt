// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: buf/validate/validate.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package com.buf.validate;

@kotlin.jvm.JvmName("-initializerule")
public inline fun rule(block: com.buf.validate.RuleKt.Dsl.() -> kotlin.Unit): com.buf.validate.Rule =
  com.buf.validate.RuleKt.Dsl._create(com.buf.validate.Rule.newBuilder()).apply { block() }._build()
/**
 * ```
 * `Rule` represents a validation rule written in the Common Expression
 * Language (CEL) syntax. Each Rule includes a unique identifier, an
 * optional error message, and the CEL expression to evaluate. For more
 * information, [see our documentation](https://buf.build/docs/protovalidate/schemas/custom-rules/).
 *
 * ```proto
 * message Foo {
 * option (buf.validate.message).cel = {
 * id: "foo.bar"
 * message: "bar must be greater than 0"
 * expression: "this.bar > 0"
 * };
 * int32 bar = 1;
 * }
 * ```
 * ```
 *
 * Protobuf type `buf.validate.Rule`
 */
public object RuleKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: com.buf.validate.Rule.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
      @kotlin.PublishedApi
      internal fun _create(builder: com.buf.validate.Rule.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
    internal fun _build(): com.buf.validate.Rule = _builder.build()

    /**
     * ```
     * `id` is a string that serves as a machine-readable name for this Rule.
     * It should be unique within its scope, which could be either a message or a field.
     * ```
     *
     * `optional string id = 1 [json_name = "id"];`
     */
    public var id: kotlin.String
      @JvmName("getId")
      get() = _builder.id
      @JvmName("setId")
      set(value) {
        _builder.id = value
      }
    /**
     * ```
     * `id` is a string that serves as a machine-readable name for this Rule.
     * It should be unique within its scope, which could be either a message or a field.
     * ```
     *
     * `optional string id = 1 [json_name = "id"];`
     */
    public fun clearId() {
      _builder.clearId()
    }
    /**
     * ```
     * `id` is a string that serves as a machine-readable name for this Rule.
     * It should be unique within its scope, which could be either a message or a field.
     * ```
     *
     * `optional string id = 1 [json_name = "id"];`
     * @return Whether the id field is set.
     */
    public fun hasId(): kotlin.Boolean {
      return _builder.hasId()
    }

    /**
     * ```
     * `message` is an optional field that provides a human-readable error message
     * for this Rule when the CEL expression evaluates to false. If a
     * non-empty message is provided, any strings resulting from the CEL
     * expression evaluation are ignored.
     * ```
     *
     * `optional string message = 2 [json_name = "message"];`
     */
    public var message: kotlin.String
      @JvmName("getMessage")
      get() = _builder.message
      @JvmName("setMessage")
      set(value) {
        _builder.message = value
      }
    /**
     * ```
     * `message` is an optional field that provides a human-readable error message
     * for this Rule when the CEL expression evaluates to false. If a
     * non-empty message is provided, any strings resulting from the CEL
     * expression evaluation are ignored.
     * ```
     *
     * `optional string message = 2 [json_name = "message"];`
     */
    public fun clearMessage() {
      _builder.clearMessage()
    }
    /**
     * ```
     * `message` is an optional field that provides a human-readable error message
     * for this Rule when the CEL expression evaluates to false. If a
     * non-empty message is provided, any strings resulting from the CEL
     * expression evaluation are ignored.
     * ```
     *
     * `optional string message = 2 [json_name = "message"];`
     * @return Whether the message field is set.
     */
    public fun hasMessage(): kotlin.Boolean {
      return _builder.hasMessage()
    }

    /**
     * ```
     * `expression` is the actual CEL expression that will be evaluated for
     * validation. This string must resolve to either a boolean or a string
     * value. If the expression evaluates to false or a non-empty string, the
     * validation is considered failed, and the message is rejected.
     * ```
     *
     * `optional string expression = 3 [json_name = "expression"];`
     */
    public var expression: kotlin.String
      @JvmName("getExpression")
      get() = _builder.expression
      @JvmName("setExpression")
      set(value) {
        _builder.expression = value
      }
    /**
     * ```
     * `expression` is the actual CEL expression that will be evaluated for
     * validation. This string must resolve to either a boolean or a string
     * value. If the expression evaluates to false or a non-empty string, the
     * validation is considered failed, and the message is rejected.
     * ```
     *
     * `optional string expression = 3 [json_name = "expression"];`
     */
    public fun clearExpression() {
      _builder.clearExpression()
    }
    /**
     * ```
     * `expression` is the actual CEL expression that will be evaluated for
     * validation. This string must resolve to either a boolean or a string
     * value. If the expression evaluates to false or a non-empty string, the
     * validation is considered failed, and the message is rejected.
     * ```
     *
     * `optional string expression = 3 [json_name = "expression"];`
     * @return Whether the expression field is set.
     */
    public fun hasExpression(): kotlin.Boolean {
      return _builder.hasExpression()
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun com.buf.validate.Rule.copy(block: `com.buf.validate`.RuleKt.Dsl.() -> kotlin.Unit): com.buf.validate.Rule =
  `com.buf.validate`.RuleKt.Dsl._create(this.toBuilder()).apply { block() }._build()

