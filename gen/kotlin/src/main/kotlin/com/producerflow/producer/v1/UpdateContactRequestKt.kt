// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: producerflow/producer/v1/producer.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package com.producerflow.producer.v1;

@kotlin.jvm.JvmName("-initializeupdateContactRequest")
public inline fun updateContactRequest(block: com.producerflow.producer.v1.UpdateContactRequestKt.Dsl.() -> kotlin.Unit): com.producerflow.producer.v1.UpdateContactRequest =
  com.producerflow.producer.v1.UpdateContactRequestKt.Dsl._create(com.producerflow.producer.v1.UpdateContactRequest.newBuilder()).apply { block() }._build()
/**
 * ```
 * UpdateContactRequest is used to update an existing contact's information.
 * ```
 *
 * Protobuf type `producerflow.producer.v1.UpdateContactRequest`
 */
public object UpdateContactRequestKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: com.producerflow.producer.v1.UpdateContactRequest.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
      @kotlin.PublishedApi
      internal fun _create(builder: com.producerflow.producer.v1.UpdateContactRequest.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
    internal fun _build(): com.producerflow.producer.v1.UpdateContactRequest = _builder.build()

    /**
     * ```
     * The UUID of the contact to update.
     * Must be a valid UUID format.
     * ```
     *
     * `string contact_id = 1 [json_name = "contactId", (.buf.validate.field) = { ... }`
     */
    public var contactId: kotlin.String
      @JvmName("getContactId")
      get() = _builder.contactId
      @JvmName("setContactId")
      set(value) {
        _builder.contactId = value
      }
    /**
     * ```
     * The UUID of the contact to update.
     * Must be a valid UUID format.
     * ```
     *
     * `string contact_id = 1 [json_name = "contactId", (.buf.validate.field) = { ... }`
     */
    public fun clearContactId() {
      _builder.clearContactId()
    }

    /**
     * ```
     * The contact information to update.
     * The field is required.
     * ```
     *
     * `.producerflow.producer.v1.UpdateContactRequest.Contact contact = 2 [json_name = "contact", (.buf.validate.field) = { ... }`
     */
    public var contact: com.producerflow.producer.v1.UpdateContactRequest.Contact
      @JvmName("getContact")
      get() = _builder.contact
      @JvmName("setContact")
      set(value) {
        _builder.contact = value
      }
    /**
     * ```
     * The contact information to update.
     * The field is required.
     * ```
     *
     * `.producerflow.producer.v1.UpdateContactRequest.Contact contact = 2 [json_name = "contact", (.buf.validate.field) = { ... }`
     */
    public fun clearContact() {
      _builder.clearContact()
    }
    /**
     * ```
     * The contact information to update.
     * The field is required.
     * ```
     *
     * `.producerflow.producer.v1.UpdateContactRequest.Contact contact = 2 [json_name = "contact", (.buf.validate.field) = { ... }`
     * @return Whether the contact field is set.
     */
    public fun hasContact(): kotlin.Boolean {
      return _builder.hasContact()
    }
    public val UpdateContactRequestKt.Dsl.contactOrNull: com.producerflow.producer.v1.UpdateContactRequest.Contact?
      get() = _builder.contactOrNull
  }
  @kotlin.jvm.JvmName("-initializecontact")
  public inline fun contact(block: com.producerflow.producer.v1.UpdateContactRequestKt.ContactKt.Dsl.() -> kotlin.Unit): com.producerflow.producer.v1.UpdateContactRequest.Contact =
    com.producerflow.producer.v1.UpdateContactRequestKt.ContactKt.Dsl._create(com.producerflow.producer.v1.UpdateContactRequest.Contact.newBuilder()).apply { block() }._build()
  /**
   * ```
   * Contact contains the fields that can be updated for a contact.
   * All fields are optional, allowing partial updates.
   * ```
   *
   * Protobuf type `producerflow.producer.v1.UpdateContactRequest.Contact`
   */
  public object ContactKt {
    @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
    @com.google.protobuf.kotlin.ProtoDslMarker
    public class Dsl private constructor(
      private val _builder: com.producerflow.producer.v1.UpdateContactRequest.Contact.Builder
    ) {
      public companion object {
        @kotlin.jvm.JvmSynthetic
        @kotlin.PublishedApi
        internal fun _create(builder: com.producerflow.producer.v1.UpdateContactRequest.Contact.Builder): Dsl = Dsl(builder)
      }

      @kotlin.jvm.JvmSynthetic
      @kotlin.PublishedApi
      internal fun _build(): com.producerflow.producer.v1.UpdateContactRequest.Contact = _builder.build()

      /**
       * ```
       * First name of the contact.
       * If provided, must be non-empty.
       * ```
       *
       * `optional string first_name = 1 [json_name = "firstName", (.buf.validate.field) = { ... }`
       */
      public var firstName: kotlin.String
        @JvmName("getFirstName")
        get() = _builder.firstName
        @JvmName("setFirstName")
        set(value) {
          _builder.firstName = value
        }
      /**
       * ```
       * First name of the contact.
       * If provided, must be non-empty.
       * ```
       *
       * `optional string first_name = 1 [json_name = "firstName", (.buf.validate.field) = { ... }`
       */
      public fun clearFirstName() {
        _builder.clearFirstName()
      }
      /**
       * ```
       * First name of the contact.
       * If provided, must be non-empty.
       * ```
       *
       * `optional string first_name = 1 [json_name = "firstName", (.buf.validate.field) = { ... }`
       * @return Whether the firstName field is set.
       */
      public fun hasFirstName(): kotlin.Boolean {
        return _builder.hasFirstName()
      }

      /**
       * ```
       * Last name of the contact.
       * If provided, must be non-empty.
       * ```
       *
       * `optional string last_name = 2 [json_name = "lastName", (.buf.validate.field) = { ... }`
       */
      public var lastName: kotlin.String
        @JvmName("getLastName")
        get() = _builder.lastName
        @JvmName("setLastName")
        set(value) {
          _builder.lastName = value
        }
      /**
       * ```
       * Last name of the contact.
       * If provided, must be non-empty.
       * ```
       *
       * `optional string last_name = 2 [json_name = "lastName", (.buf.validate.field) = { ... }`
       */
      public fun clearLastName() {
        _builder.clearLastName()
      }
      /**
       * ```
       * Last name of the contact.
       * If provided, must be non-empty.
       * ```
       *
       * `optional string last_name = 2 [json_name = "lastName", (.buf.validate.field) = { ... }`
       * @return Whether the lastName field is set.
       */
      public fun hasLastName(): kotlin.Boolean {
        return _builder.hasLastName()
      }

      /**
       * ```
       * Middle name of the contact.
       * If provided, must be non-empty.
       * ```
       *
       * `optional string middle_name = 3 [json_name = "middleName", (.buf.validate.field) = { ... }`
       */
      public var middleName: kotlin.String
        @JvmName("getMiddleName")
        get() = _builder.middleName
        @JvmName("setMiddleName")
        set(value) {
          _builder.middleName = value
        }
      /**
       * ```
       * Middle name of the contact.
       * If provided, must be non-empty.
       * ```
       *
       * `optional string middle_name = 3 [json_name = "middleName", (.buf.validate.field) = { ... }`
       */
      public fun clearMiddleName() {
        _builder.clearMiddleName()
      }
      /**
       * ```
       * Middle name of the contact.
       * If provided, must be non-empty.
       * ```
       *
       * `optional string middle_name = 3 [json_name = "middleName", (.buf.validate.field) = { ... }`
       * @return Whether the middleName field is set.
       */
      public fun hasMiddleName(): kotlin.Boolean {
        return _builder.hasMiddleName()
      }

      /**
       * ```
       * Email address of the contact.
       * If provided, must be a valid email format.
       * Must be unique within the tenant.
       * ```
       *
       * `optional string email = 4 [json_name = "email", (.buf.validate.field) = { ... }`
       */
      public var email: kotlin.String
        @JvmName("getEmail")
        get() = _builder.email
        @JvmName("setEmail")
        set(value) {
          _builder.email = value
        }
      /**
       * ```
       * Email address of the contact.
       * If provided, must be a valid email format.
       * Must be unique within the tenant.
       * ```
       *
       * `optional string email = 4 [json_name = "email", (.buf.validate.field) = { ... }`
       */
      public fun clearEmail() {
        _builder.clearEmail()
      }
      /**
       * ```
       * Email address of the contact.
       * If provided, must be a valid email format.
       * Must be unique within the tenant.
       * ```
       *
       * `optional string email = 4 [json_name = "email", (.buf.validate.field) = { ... }`
       * @return Whether the email field is set.
       */
      public fun hasEmail(): kotlin.Boolean {
        return _builder.hasEmail()
      }

      /**
       * ```
       * Phone number of the contact.
       * If provided, must be a valid phone number format.
       * ```
       *
       * `optional string phone = 5 [json_name = "phone", (.buf.validate.field) = { ... }`
       */
      public var phone: kotlin.String
        @JvmName("getPhone")
        get() = _builder.phone
        @JvmName("setPhone")
        set(value) {
          _builder.phone = value
        }
      /**
       * ```
       * Phone number of the contact.
       * If provided, must be a valid phone number format.
       * ```
       *
       * `optional string phone = 5 [json_name = "phone", (.buf.validate.field) = { ... }`
       */
      public fun clearPhone() {
        _builder.clearPhone()
      }
      /**
       * ```
       * Phone number of the contact.
       * If provided, must be a valid phone number format.
       * ```
       *
       * `optional string phone = 5 [json_name = "phone", (.buf.validate.field) = { ... }`
       * @return Whether the phone field is set.
       */
      public fun hasPhone(): kotlin.Boolean {
        return _builder.hasPhone()
      }

      /**
       * ```
       * Role or position of the contact within the agency.
       * If provided, must be a valid ContactRole enum value (not UNSPECIFIED).
       * See ContactRole enum for available options.
       * ```
       *
       * `optional .producerflow.producer.v1.ContactRole role = 6 [json_name = "role", (.buf.validate.field) = { ... }`
       */
      public var role: com.producerflow.producer.v1.ContactRole
        @JvmName("getRole")
        get() = _builder.role
        @JvmName("setRole")
        set(value) {
          _builder.role = value
        }
      public var roleValue: kotlin.Int
        @JvmName("getRoleValue")
        get() = _builder.roleValue
        @JvmName("setRoleValue")
        set(value) {
          _builder.roleValue = value
        }
      /**
       * ```
       * Role or position of the contact within the agency.
       * If provided, must be a valid ContactRole enum value (not UNSPECIFIED).
       * See ContactRole enum for available options.
       * ```
       *
       * `optional .producerflow.producer.v1.ContactRole role = 6 [json_name = "role", (.buf.validate.field) = { ... }`
       */
      public fun clearRole() {
        _builder.clearRole()
      }
      /**
       * ```
       * Role or position of the contact within the agency.
       * If provided, must be a valid ContactRole enum value (not UNSPECIFIED).
       * See ContactRole enum for available options.
       * ```
       *
       * `optional .producerflow.producer.v1.ContactRole role = 6 [json_name = "role", (.buf.validate.field) = { ... }`
       * @return Whether the role field is set.
       */
      public fun hasRole(): kotlin.Boolean {
        return _builder.hasRole()
      }

      /**
       * ```
       * Mailing address of the contact.
       * If provided, all address fields should be included.
       * This replaces the entire address.
       * ```
       *
       * `optional .producerflow.producer.v1.Address address = 7 [json_name = "address"];`
       */
      public var address: com.producerflow.producer.v1.Address
        @JvmName("getAddress")
        get() = _builder.address
        @JvmName("setAddress")
        set(value) {
          _builder.address = value
        }
      /**
       * ```
       * Mailing address of the contact.
       * If provided, all address fields should be included.
       * This replaces the entire address.
       * ```
       *
       * `optional .producerflow.producer.v1.Address address = 7 [json_name = "address"];`
       */
      public fun clearAddress() {
        _builder.clearAddress()
      }
      /**
       * ```
       * Mailing address of the contact.
       * If provided, all address fields should be included.
       * This replaces the entire address.
       * ```
       *
       * `optional .producerflow.producer.v1.Address address = 7 [json_name = "address"];`
       * @return Whether the address field is set.
       */
      public fun hasAddress(): kotlin.Boolean {
        return _builder.hasAddress()
      }
      public val ContactKt.Dsl.addressOrNull: com.producerflow.producer.v1.Address?
        get() = _builder.addressOrNull

      /**
       * An uninstantiable, behaviorless type to represent the field in
       * generics.
       */
      @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
      public class ExternalMetadataProxy private constructor() : com.google.protobuf.kotlin.DslProxy()
      /**
       * ```
       * ExternalMetadata contains additional custom information that the tenant stores in ProducerFlow's data model.
       * This field allows tenants to attach arbitrary key-value pairs to contacts for their own business logic,
       * reporting, or integration needs. This field is populated programmatically via API calls by the tenant's systems.
       * Common use cases include:
       * - Storing references to external system states or categories
       * - Adding custom tags or classifications
       * - Maintaining tenant-specific business attributes
       * - Storing computed values or derived data
       * The map key is the metadata field name, and the value is the associated data.
       *
       * Update behavior:
       * - If not provided (null): existing metadata is preserved unchanged
       * - If provided as empty map {}: existing metadata is cleared
       * - If provided with values: existing metadata is completely replaced with the new values
       * ```
       *
       * `map<string, string> external_metadata = 8 [json_name = "externalMetadata"];`
       */
       public val externalMetadata: com.google.protobuf.kotlin.DslMap<kotlin.String, kotlin.String, ExternalMetadataProxy>
        @kotlin.jvm.JvmSynthetic
        @JvmName("getExternalMetadataMap")
        get() = com.google.protobuf.kotlin.DslMap(
          _builder.externalMetadataMap
        )
      /**
       * ```
       * ExternalMetadata contains additional custom information that the tenant stores in ProducerFlow's data model.
       * This field allows tenants to attach arbitrary key-value pairs to contacts for their own business logic,
       * reporting, or integration needs. This field is populated programmatically via API calls by the tenant's systems.
       * Common use cases include:
       * - Storing references to external system states or categories
       * - Adding custom tags or classifications
       * - Maintaining tenant-specific business attributes
       * - Storing computed values or derived data
       * The map key is the metadata field name, and the value is the associated data.
       *
       * Update behavior:
       * - If not provided (null): existing metadata is preserved unchanged
       * - If provided as empty map {}: existing metadata is cleared
       * - If provided with values: existing metadata is completely replaced with the new values
       * ```
       *
       * `map<string, string> external_metadata = 8 [json_name = "externalMetadata"];`
       */
      @JvmName("putExternalMetadata")
      public fun com.google.protobuf.kotlin.DslMap<kotlin.String, kotlin.String, ExternalMetadataProxy>
        .put(key: kotlin.String, value: kotlin.String) {
           _builder.putExternalMetadata(key, value)
         }
      /**
       * ```
       * ExternalMetadata contains additional custom information that the tenant stores in ProducerFlow's data model.
       * This field allows tenants to attach arbitrary key-value pairs to contacts for their own business logic,
       * reporting, or integration needs. This field is populated programmatically via API calls by the tenant's systems.
       * Common use cases include:
       * - Storing references to external system states or categories
       * - Adding custom tags or classifications
       * - Maintaining tenant-specific business attributes
       * - Storing computed values or derived data
       * The map key is the metadata field name, and the value is the associated data.
       *
       * Update behavior:
       * - If not provided (null): existing metadata is preserved unchanged
       * - If provided as empty map {}: existing metadata is cleared
       * - If provided with values: existing metadata is completely replaced with the new values
       * ```
       *
       * `map<string, string> external_metadata = 8 [json_name = "externalMetadata"];`
       */
      @kotlin.jvm.JvmSynthetic
      @JvmName("setExternalMetadata")
      @Suppress("NOTHING_TO_INLINE")
      public inline operator fun com.google.protobuf.kotlin.DslMap<kotlin.String, kotlin.String, ExternalMetadataProxy>
        .set(key: kotlin.String, value: kotlin.String) {
           put(key, value)
         }
      /**
       * ```
       * ExternalMetadata contains additional custom information that the tenant stores in ProducerFlow's data model.
       * This field allows tenants to attach arbitrary key-value pairs to contacts for their own business logic,
       * reporting, or integration needs. This field is populated programmatically via API calls by the tenant's systems.
       * Common use cases include:
       * - Storing references to external system states or categories
       * - Adding custom tags or classifications
       * - Maintaining tenant-specific business attributes
       * - Storing computed values or derived data
       * The map key is the metadata field name, and the value is the associated data.
       *
       * Update behavior:
       * - If not provided (null): existing metadata is preserved unchanged
       * - If provided as empty map {}: existing metadata is cleared
       * - If provided with values: existing metadata is completely replaced with the new values
       * ```
       *
       * `map<string, string> external_metadata = 8 [json_name = "externalMetadata"];`
       */
      @kotlin.jvm.JvmSynthetic
      @JvmName("removeExternalMetadata")
      public fun com.google.protobuf.kotlin.DslMap<kotlin.String, kotlin.String, ExternalMetadataProxy>
        .remove(key: kotlin.String) {
           _builder.removeExternalMetadata(key)
         }
      /**
       * ```
       * ExternalMetadata contains additional custom information that the tenant stores in ProducerFlow's data model.
       * This field allows tenants to attach arbitrary key-value pairs to contacts for their own business logic,
       * reporting, or integration needs. This field is populated programmatically via API calls by the tenant's systems.
       * Common use cases include:
       * - Storing references to external system states or categories
       * - Adding custom tags or classifications
       * - Maintaining tenant-specific business attributes
       * - Storing computed values or derived data
       * The map key is the metadata field name, and the value is the associated data.
       *
       * Update behavior:
       * - If not provided (null): existing metadata is preserved unchanged
       * - If provided as empty map {}: existing metadata is cleared
       * - If provided with values: existing metadata is completely replaced with the new values
       * ```
       *
       * `map<string, string> external_metadata = 8 [json_name = "externalMetadata"];`
       */
      @kotlin.jvm.JvmSynthetic
      @JvmName("putAllExternalMetadata")
      public fun com.google.protobuf.kotlin.DslMap<kotlin.String, kotlin.String, ExternalMetadataProxy>
        .putAll(map: kotlin.collections.Map<kotlin.String, kotlin.String>) {
           _builder.putAllExternalMetadata(map)
         }
      /**
       * ```
       * ExternalMetadata contains additional custom information that the tenant stores in ProducerFlow's data model.
       * This field allows tenants to attach arbitrary key-value pairs to contacts for their own business logic,
       * reporting, or integration needs. This field is populated programmatically via API calls by the tenant's systems.
       * Common use cases include:
       * - Storing references to external system states or categories
       * - Adding custom tags or classifications
       * - Maintaining tenant-specific business attributes
       * - Storing computed values or derived data
       * The map key is the metadata field name, and the value is the associated data.
       *
       * Update behavior:
       * - If not provided (null): existing metadata is preserved unchanged
       * - If provided as empty map {}: existing metadata is cleared
       * - If provided with values: existing metadata is completely replaced with the new values
       * ```
       *
       * `map<string, string> external_metadata = 8 [json_name = "externalMetadata"];`
       */
      @kotlin.jvm.JvmSynthetic
      @JvmName("clearExternalMetadata")
      public fun com.google.protobuf.kotlin.DslMap<kotlin.String, kotlin.String, ExternalMetadataProxy>
        .clear() {
           _builder.clearExternalMetadata()
         }
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun com.producerflow.producer.v1.UpdateContactRequest.copy(block: `com.producerflow.producer.v1`.UpdateContactRequestKt.Dsl.() -> kotlin.Unit): com.producerflow.producer.v1.UpdateContactRequest =
  `com.producerflow.producer.v1`.UpdateContactRequestKt.Dsl._create(this.toBuilder()).apply { block() }._build()

@kotlin.jvm.JvmSynthetic
public inline fun com.producerflow.producer.v1.UpdateContactRequest.Contact.copy(block: `com.producerflow.producer.v1`.UpdateContactRequestKt.ContactKt.Dsl.() -> kotlin.Unit): com.producerflow.producer.v1.UpdateContactRequest.Contact =
  `com.producerflow.producer.v1`.UpdateContactRequestKt.ContactKt.Dsl._create(this.toBuilder()).apply { block() }._build()

public val com.producerflow.producer.v1.UpdateContactRequest.ContactOrBuilder.addressOrNull: com.producerflow.producer.v1.Address?
  get() = if (hasAddress()) getAddress() else null

public val com.producerflow.producer.v1.UpdateContactRequestOrBuilder.contactOrNull: com.producerflow.producer.v1.UpdateContactRequest.Contact?
  get() = if (hasContact()) getContact() else null

