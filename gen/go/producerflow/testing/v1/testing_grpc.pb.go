// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: producerflow/testing/v1/testing.proto

package testingv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	TestingService_DeleteAgency_FullMethodName      = "/producerflow.testing.v1.TestingService/DeleteAgency"
	TestingService_DeleteAppointment_FullMethodName = "/producerflow.testing.v1.TestingService/DeleteAppointment"
)

// TestingServiceClient is the client API for TestingService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ============================================================================
// DEV AND UAT ONLY — NOT AVAILABLE IN PRODUCTION.
// Every method on this service is enabled exclusively in the dev and UAT
// test environments. In production, every call is rejected with
// PERMISSION_DENIED, regardless of the API key or arguments.
// ============================================================================
//
// TestingService provides cleanup utilities that let tenants reset their test
// environments between automated runs. For example, deleting an agency frees
// the licenses it used so the same test setup can be reused across test cases
// instead of provisioning new agencies each time. These operations are
// intended solely for preparing and cleaning up automated test scenarios and
// have no production use.
//
// Any call to this service must be authenticated using an API key in the
// request headers. The API key can be found in the ProducerFlow API key
// section of the ProducerFlow UI and it identifies the tenant that is making
// the request.
type TestingServiceClient interface {
	// DeleteAgency permanently removes an agency and every producer,
	// appointment, and NIPR record scoped to it, freeing the underlying
	// licenses to be appointed again. It lets tenants reset the same agency
	// between automated test runs instead of provisioning a new one each time.
	//
	// DEV AND UAT ONLY: this method is not available in production. Calls made
	// in production are always rejected with PERMISSION_DENIED.
	//
	// The agency must belong to the tenant resolved from the request's API key.
	// Tenant (internal) agencies cannot be deleted — they hold per-tenant
	// configuration. The operation is irreversible and emits no events.
	//
	// Errors:
	// - NOT_FOUND: no agency with the given ID exists for the tenant.
	// - FAILED_PRECONDITION: the agency is a tenant (internal) agency.
	// - INVALID_ARGUMENT: agency_id is missing or is not a valid UUID.
	// - PERMISSION_DENIED: the call was made outside dev/UAT (e.g. production).
	DeleteAgency(ctx context.Context, in *DeleteAgencyRequest, opts ...grpc.CallOption) (*DeleteAgencyResponse, error)
	// DeleteAppointment permanently removes a single appointment, freeing the
	// underlying license to be appointed again. Unlike terminating an
	// appointment — which leaves a TERMINATED record that still blocks the
	// license from being re-appointed — deleting it removes the row entirely so
	// the same license can be reused across automated test runs.
	//
	// DEV AND UAT ONLY: this method is not available in production. Calls made
	// in production are always rejected with PERMISSION_DENIED.
	//
	// The appointment must belong to the tenant resolved from the request's API
	// key. Its interleaved history is removed with it. The operation is
	// irreversible, emits no events, and sends nothing to NIPR.
	//
	// Errors:
	// - NOT_FOUND: no appointment with the given ID exists for the tenant.
	// - INVALID_ARGUMENT: appointment_id is missing or is not a valid UUID.
	// - PERMISSION_DENIED: the call was made outside dev/UAT (e.g. production).
	DeleteAppointment(ctx context.Context, in *DeleteAppointmentRequest, opts ...grpc.CallOption) (*DeleteAppointmentResponse, error)
}

type testingServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewTestingServiceClient(cc grpc.ClientConnInterface) TestingServiceClient {
	return &testingServiceClient{cc}
}

func (c *testingServiceClient) DeleteAgency(ctx context.Context, in *DeleteAgencyRequest, opts ...grpc.CallOption) (*DeleteAgencyResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(DeleteAgencyResponse)
	err := c.cc.Invoke(ctx, TestingService_DeleteAgency_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *testingServiceClient) DeleteAppointment(ctx context.Context, in *DeleteAppointmentRequest, opts ...grpc.CallOption) (*DeleteAppointmentResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(DeleteAppointmentResponse)
	err := c.cc.Invoke(ctx, TestingService_DeleteAppointment_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// TestingServiceServer is the server API for TestingService service.
// All implementations must embed UnimplementedTestingServiceServer
// for forward compatibility.
//
// ============================================================================
// DEV AND UAT ONLY — NOT AVAILABLE IN PRODUCTION.
// Every method on this service is enabled exclusively in the dev and UAT
// test environments. In production, every call is rejected with
// PERMISSION_DENIED, regardless of the API key or arguments.
// ============================================================================
//
// TestingService provides cleanup utilities that let tenants reset their test
// environments between automated runs. For example, deleting an agency frees
// the licenses it used so the same test setup can be reused across test cases
// instead of provisioning new agencies each time. These operations are
// intended solely for preparing and cleaning up automated test scenarios and
// have no production use.
//
// Any call to this service must be authenticated using an API key in the
// request headers. The API key can be found in the ProducerFlow API key
// section of the ProducerFlow UI and it identifies the tenant that is making
// the request.
type TestingServiceServer interface {
	// DeleteAgency permanently removes an agency and every producer,
	// appointment, and NIPR record scoped to it, freeing the underlying
	// licenses to be appointed again. It lets tenants reset the same agency
	// between automated test runs instead of provisioning a new one each time.
	//
	// DEV AND UAT ONLY: this method is not available in production. Calls made
	// in production are always rejected with PERMISSION_DENIED.
	//
	// The agency must belong to the tenant resolved from the request's API key.
	// Tenant (internal) agencies cannot be deleted — they hold per-tenant
	// configuration. The operation is irreversible and emits no events.
	//
	// Errors:
	// - NOT_FOUND: no agency with the given ID exists for the tenant.
	// - FAILED_PRECONDITION: the agency is a tenant (internal) agency.
	// - INVALID_ARGUMENT: agency_id is missing or is not a valid UUID.
	// - PERMISSION_DENIED: the call was made outside dev/UAT (e.g. production).
	DeleteAgency(context.Context, *DeleteAgencyRequest) (*DeleteAgencyResponse, error)
	// DeleteAppointment permanently removes a single appointment, freeing the
	// underlying license to be appointed again. Unlike terminating an
	// appointment — which leaves a TERMINATED record that still blocks the
	// license from being re-appointed — deleting it removes the row entirely so
	// the same license can be reused across automated test runs.
	//
	// DEV AND UAT ONLY: this method is not available in production. Calls made
	// in production are always rejected with PERMISSION_DENIED.
	//
	// The appointment must belong to the tenant resolved from the request's API
	// key. Its interleaved history is removed with it. The operation is
	// irreversible, emits no events, and sends nothing to NIPR.
	//
	// Errors:
	// - NOT_FOUND: no appointment with the given ID exists for the tenant.
	// - INVALID_ARGUMENT: appointment_id is missing or is not a valid UUID.
	// - PERMISSION_DENIED: the call was made outside dev/UAT (e.g. production).
	DeleteAppointment(context.Context, *DeleteAppointmentRequest) (*DeleteAppointmentResponse, error)
	mustEmbedUnimplementedTestingServiceServer()
}

// UnimplementedTestingServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedTestingServiceServer struct{}

func (UnimplementedTestingServiceServer) DeleteAgency(context.Context, *DeleteAgencyRequest) (*DeleteAgencyResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method DeleteAgency not implemented")
}
func (UnimplementedTestingServiceServer) DeleteAppointment(context.Context, *DeleteAppointmentRequest) (*DeleteAppointmentResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method DeleteAppointment not implemented")
}
func (UnimplementedTestingServiceServer) mustEmbedUnimplementedTestingServiceServer() {}
func (UnimplementedTestingServiceServer) testEmbeddedByValue()                        {}

// UnsafeTestingServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to TestingServiceServer will
// result in compilation errors.
type UnsafeTestingServiceServer interface {
	mustEmbedUnimplementedTestingServiceServer()
}

func RegisterTestingServiceServer(s grpc.ServiceRegistrar, srv TestingServiceServer) {
	// If the following call pancis, it indicates UnimplementedTestingServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&TestingService_ServiceDesc, srv)
}

func _TestingService_DeleteAgency_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(DeleteAgencyRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(TestingServiceServer).DeleteAgency(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: TestingService_DeleteAgency_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(TestingServiceServer).DeleteAgency(ctx, req.(*DeleteAgencyRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _TestingService_DeleteAppointment_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(DeleteAppointmentRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(TestingServiceServer).DeleteAppointment(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: TestingService_DeleteAppointment_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(TestingServiceServer).DeleteAppointment(ctx, req.(*DeleteAppointmentRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// TestingService_ServiceDesc is the grpc.ServiceDesc for TestingService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var TestingService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "producerflow.testing.v1.TestingService",
	HandlerType: (*TestingServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "DeleteAgency",
			Handler:    _TestingService_DeleteAgency_Handler,
		},
		{
			MethodName: "DeleteAppointment",
			Handler:    _TestingService_DeleteAppointment_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "producerflow/testing/v1/testing.proto",
}
