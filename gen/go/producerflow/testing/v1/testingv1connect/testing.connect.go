// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: producerflow/testing/v1/testing.proto

package testingv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/producerflow/producerflowapi/gen/go/producerflow/testing/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TestingServiceName is the fully-qualified name of the TestingService service.
	TestingServiceName = "producerflow.testing.v1.TestingService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TestingServiceDeleteAgencyProcedure is the fully-qualified name of the TestingService's
	// DeleteAgency RPC.
	TestingServiceDeleteAgencyProcedure = "/producerflow.testing.v1.TestingService/DeleteAgency"
	// TestingServiceDeleteAppointmentProcedure is the fully-qualified name of the TestingService's
	// DeleteAppointment RPC.
	TestingServiceDeleteAppointmentProcedure = "/producerflow.testing.v1.TestingService/DeleteAppointment"
)

// TestingServiceClient is a client for the producerflow.testing.v1.TestingService service.
type TestingServiceClient interface {
	// DeleteAgency permanently removes an agency and every producer,
	// appointment, and NIPR record scoped to it, freeing the underlying
	// licenses to be appointed again. It lets tenants reset the same agency
	// between automated test runs instead of provisioning a new one each time.
	//
	// DEV AND UAT ONLY: this method is not available in production. Calls made
	// in production are always rejected with PERMISSION_DENIED.
	//
	// The agency must belong to the tenant resolved from the request's API key.
	// Tenant (internal) agencies cannot be deleted — they hold per-tenant
	// configuration. The operation is irreversible and emits no events.
	//
	// Errors:
	// - NOT_FOUND: no agency with the given ID exists for the tenant.
	// - FAILED_PRECONDITION: the agency is a tenant (internal) agency.
	// - INVALID_ARGUMENT: agency_id is missing or is not a valid UUID.
	// - PERMISSION_DENIED: the call was made outside dev/UAT (e.g. production).
	DeleteAgency(context.Context, *connect.Request[v1.DeleteAgencyRequest]) (*connect.Response[v1.DeleteAgencyResponse], error)
	// DeleteAppointment permanently removes a single appointment, freeing the
	// underlying license to be appointed again. Unlike terminating an
	// appointment — which leaves a TERMINATED record that still blocks the
	// license from being re-appointed — deleting it removes the row entirely so
	// the same license can be reused across automated test runs.
	//
	// DEV AND UAT ONLY: this method is not available in production. Calls made
	// in production are always rejected with PERMISSION_DENIED.
	//
	// The appointment must belong to the tenant resolved from the request's API
	// key. Its interleaved history is removed with it. The operation is
	// irreversible, emits no events, and sends nothing to NIPR.
	//
	// Errors:
	// - NOT_FOUND: no appointment with the given ID exists for the tenant.
	// - INVALID_ARGUMENT: appointment_id is missing or is not a valid UUID.
	// - PERMISSION_DENIED: the call was made outside dev/UAT (e.g. production).
	DeleteAppointment(context.Context, *connect.Request[v1.DeleteAppointmentRequest]) (*connect.Response[v1.DeleteAppointmentResponse], error)
}

// NewTestingServiceClient constructs a client for the producerflow.testing.v1.TestingService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTestingServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TestingServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	testingServiceMethods := v1.File_producerflow_testing_v1_testing_proto.Services().ByName("TestingService").Methods()
	return &testingServiceClient{
		deleteAgency: connect.NewClient[v1.DeleteAgencyRequest, v1.DeleteAgencyResponse](
			httpClient,
			baseURL+TestingServiceDeleteAgencyProcedure,
			connect.WithSchema(testingServiceMethods.ByName("DeleteAgency")),
			connect.WithClientOptions(opts...),
		),
		deleteAppointment: connect.NewClient[v1.DeleteAppointmentRequest, v1.DeleteAppointmentResponse](
			httpClient,
			baseURL+TestingServiceDeleteAppointmentProcedure,
			connect.WithSchema(testingServiceMethods.ByName("DeleteAppointment")),
			connect.WithClientOptions(opts...),
		),
	}
}

// testingServiceClient implements TestingServiceClient.
type testingServiceClient struct {
	deleteAgency      *connect.Client[v1.DeleteAgencyRequest, v1.DeleteAgencyResponse]
	deleteAppointment *connect.Client[v1.DeleteAppointmentRequest, v1.DeleteAppointmentResponse]
}

// DeleteAgency calls producerflow.testing.v1.TestingService.DeleteAgency.
func (c *testingServiceClient) DeleteAgency(ctx context.Context, req *connect.Request[v1.DeleteAgencyRequest]) (*connect.Response[v1.DeleteAgencyResponse], error) {
	return c.deleteAgency.CallUnary(ctx, req)
}

// DeleteAppointment calls producerflow.testing.v1.TestingService.DeleteAppointment.
func (c *testingServiceClient) DeleteAppointment(ctx context.Context, req *connect.Request[v1.DeleteAppointmentRequest]) (*connect.Response[v1.DeleteAppointmentResponse], error) {
	return c.deleteAppointment.CallUnary(ctx, req)
}

// TestingServiceHandler is an implementation of the producerflow.testing.v1.TestingService service.
type TestingServiceHandler interface {
	// DeleteAgency permanently removes an agency and every producer,
	// appointment, and NIPR record scoped to it, freeing the underlying
	// licenses to be appointed again. It lets tenants reset the same agency
	// between automated test runs instead of provisioning a new one each time.
	//
	// DEV AND UAT ONLY: this method is not available in production. Calls made
	// in production are always rejected with PERMISSION_DENIED.
	//
	// The agency must belong to the tenant resolved from the request's API key.
	// Tenant (internal) agencies cannot be deleted — they hold per-tenant
	// configuration. The operation is irreversible and emits no events.
	//
	// Errors:
	// - NOT_FOUND: no agency with the given ID exists for the tenant.
	// - FAILED_PRECONDITION: the agency is a tenant (internal) agency.
	// - INVALID_ARGUMENT: agency_id is missing or is not a valid UUID.
	// - PERMISSION_DENIED: the call was made outside dev/UAT (e.g. production).
	DeleteAgency(context.Context, *connect.Request[v1.DeleteAgencyRequest]) (*connect.Response[v1.DeleteAgencyResponse], error)
	// DeleteAppointment permanently removes a single appointment, freeing the
	// underlying license to be appointed again. Unlike terminating an
	// appointment — which leaves a TERMINATED record that still blocks the
	// license from being re-appointed — deleting it removes the row entirely so
	// the same license can be reused across automated test runs.
	//
	// DEV AND UAT ONLY: this method is not available in production. Calls made
	// in production are always rejected with PERMISSION_DENIED.
	//
	// The appointment must belong to the tenant resolved from the request's API
	// key. Its interleaved history is removed with it. The operation is
	// irreversible, emits no events, and sends nothing to NIPR.
	//
	// Errors:
	// - NOT_FOUND: no appointment with the given ID exists for the tenant.
	// - INVALID_ARGUMENT: appointment_id is missing or is not a valid UUID.
	// - PERMISSION_DENIED: the call was made outside dev/UAT (e.g. production).
	DeleteAppointment(context.Context, *connect.Request[v1.DeleteAppointmentRequest]) (*connect.Response[v1.DeleteAppointmentResponse], error)
}

// NewTestingServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTestingServiceHandler(svc TestingServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	testingServiceMethods := v1.File_producerflow_testing_v1_testing_proto.Services().ByName("TestingService").Methods()
	testingServiceDeleteAgencyHandler := connect.NewUnaryHandler(
		TestingServiceDeleteAgencyProcedure,
		svc.DeleteAgency,
		connect.WithSchema(testingServiceMethods.ByName("DeleteAgency")),
		connect.WithHandlerOptions(opts...),
	)
	testingServiceDeleteAppointmentHandler := connect.NewUnaryHandler(
		TestingServiceDeleteAppointmentProcedure,
		svc.DeleteAppointment,
		connect.WithSchema(testingServiceMethods.ByName("DeleteAppointment")),
		connect.WithHandlerOptions(opts...),
	)
	return "/producerflow.testing.v1.TestingService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TestingServiceDeleteAgencyProcedure:
			testingServiceDeleteAgencyHandler.ServeHTTP(w, r)
		case TestingServiceDeleteAppointmentProcedure:
			testingServiceDeleteAppointmentHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTestingServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedTestingServiceHandler struct{}

func (UnimplementedTestingServiceHandler) DeleteAgency(context.Context, *connect.Request[v1.DeleteAgencyRequest]) (*connect.Response[v1.DeleteAgencyResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("producerflow.testing.v1.TestingService.DeleteAgency is not implemented"))
}

func (UnimplementedTestingServiceHandler) DeleteAppointment(context.Context, *connect.Request[v1.DeleteAppointmentRequest]) (*connect.Response[v1.DeleteAppointmentResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("producerflow.testing.v1.TestingService.DeleteAppointment is not implemented"))
}
